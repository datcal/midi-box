# Core (works on Mac + Pi)
mido>=1.3.0
python-rtmidi>=1.5.0
flask>=3.0
pyyaml>=6.0
qrcode>=7.4          # QR code generation for WiFi connect display
psutil>=5.9          # CPU / RAM / disk stats for the system page
zeroconf>=0.47       # mDNS/Bonjour advertisement for RTP-MIDI auto-discovery

# Hardware MIDI serial (used on Pi for SC16IS752, optional on Mac)
pyserial>=3.5

# Pi-only (install manually on Raspberry Pi)
# RPi.GPIO>=0.7
# smbus2>=0.4
